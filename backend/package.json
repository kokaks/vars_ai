{
  "name": "barbershop-ai-backend",
  "version": "0.1.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "reset-db": "node -e \"require('fs').unlinkSync(require('path').join(__dirname,'db','barbershop.db'))\" && echo 'DB deleted — will reseed on next start'"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "luxon": "^3.5.0",
    "pg": "^8.13.1",
    "web-push": "^3.6.7"
  }
}
